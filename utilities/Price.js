class Price {
    constructor(dT, op, hp, lp, cp) {
        this.dateTime = dT;
        this.openPrice = op;
        this.highPrice = hp;
        this.lowPrice = lp;
        this.closePrice = cp;
    }
};
    